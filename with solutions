# Write a Python function that takes a list of words and returns the longest word and the length of the longest one
list_words = ['heaven', 'Exercises']
longest_word = max(list_words, key=len)
length = len(longest_word)
print(longest_word)
print(length)
